{
  "name": "Dnols marketplace PWA",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "api",
      "command": "cd api && npm start",
      "description": "Fastify escrow API on http://localhost:8787 (distance-only listings; reveals shop location only after mock payment)."
    },
    {
      "name": "web",
      "command": "npm run dev",
      "description": "Buyer site + PWA (Vite) on http://localhost:5173. `/` marketing landing; `/app` shopper. Proxies /api -> :8787. Market QR: http://localhost:5173/app?place=place_kariakoo_dsm"
    },
    {
      "name": "shop",
      "command": "cd shop && npm run dev",
      "description": "Seller/stall PWA (Vite) on http://localhost:5174. Proxies /api -> :8787."
    }
  ],
  "ports": [
    { "name": "api", "port": 8787 },
    { "name": "web (buyer site + PWA)", "port": 5173 },
    { "name": "shop (seller PWA)", "port": 5174 }
  ]
}
