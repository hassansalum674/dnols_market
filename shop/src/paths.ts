/** Seller chrome lives at `/shop` on the root Vite origin. */
export const shopPaths = {
  home: "/shop",
  stock: "/shop/stock",
  orders: "/shop/orders",
  profile: "/shop/profile",
  buyer: "/app",
} as const;
